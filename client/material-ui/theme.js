import { createTheme } from '@material-ui/core/styles';

const theme = createTheme({
  palette: {
    primary: {
      // main: '#317bf5'
      main: '#2196f3'
    },
    secondary: {
      main: '#ffd54f'
    },
  },
});

export default theme;